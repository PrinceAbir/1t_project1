// keep a small shim so any leftover imports don't break until fully removed
import React from 'react';
export default function MetaBuilder() { return (
  <div style={{padding:20}}>MetaBuilder has moved. Open <a href="/etd-designer">ETDDesigner</a>.</div>
);
}
